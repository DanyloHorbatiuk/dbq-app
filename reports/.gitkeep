Generated by app/benchmark.py and scripts/*.py — never edit by hand
(CLAUDE.md). Kept tracked only so this directory exists after a fresh
checkout, for docker-compose's ./reports:/srv/reports bind mount.
